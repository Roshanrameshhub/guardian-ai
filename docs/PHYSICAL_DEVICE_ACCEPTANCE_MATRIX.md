# Guardian AI — Physical Device Acceptance Matrix

**Audit Date:** 2026-08-19  
**Target Platform:** Physical Android Phone (SDK 34+)  
**App ID / Package:** `com.guardianai.guardian_ai`  
**Firebase Project:** `guardian-ai-505705`  
**Audit Rule:** Never use `PASS` without verified physical hardware execution. Automated tests qualify as `SIMULATED_ONLY` or `NOT_TESTED`. External missing credentials qualify as `BLOCKED`.

---

## Acceptance Matrix

| Feature | Backend API | Flutter Service | Permission | Background Requirement | Physical Test | Expected Result | Actual Result | Status | Evidence/Log |
|---|---|---|---|---|---|---|---|---|---|
| **1. Email/Password Auth** | `POST /api/v1/auth/login` | `AuthService`, `TokenStorageService`, `ApiClient` | None | None | Login with registered credentials on device | JWT returned, saved to SecureStorage, ApiClient attaches `Bearer <token>`, dashboard loads HTTP 200 | Verified in test & client code; requires live backend connection | `SIMULATED_ONLY` | `test/api_integration_test.dart` passes; `ApiClient` attaches `Bearer` header |
| **2. Auth 401 Rejection** | `POST /api/v1/auth/login` | `ApiClient` | None | None | Attempt login with invalid credentials | HTTP 401 with `{"detail": "Invalid email or password"}` displayed truthfully without "Unexpected error" | Backend returns `InvalidCredentialsError` (401); ApiClient parses `detail` | `SIMULATED_ONLY` | `AuthException` with message extracted from response body |
| **3. Google OAuth** | `POST /api/v1/auth/google` | `AuthService`, `google_sign_in` | Internet | None | Tap "Continue with Google" on physical phone | Account picker opens, Google ID token received, verified by backend, user logged in | `google-services.json` has `"oauth_client": []`. Missing SHA-1/SHA-256 registration causes `ApiException 10` on physical device | `BLOCKED` | `android/app/google-services.json` line 15: `"oauth_client": []` requires Firebase SHA configuration |
| **4. Dashboard Token Restore** | `GET /api/v1/dashboard` | `DashboardRepository`, `TokenStorageService` | None | None | Reopen app after force close with valid stored token | Splash restores token, ApiClient attaches Bearer header, loads live dashboard | Token restored from `FlutterSecureStorage`, calls `GET /api/v1/dashboard` | `SIMULATED_ONLY` | `DashboardEntity` deserialized from live payload |
| **5. API URL Resolution** | `GET /api/v1/health` | `ApiConfig`, `ApiClient` | Internet | None | Ping backend from physical device via configured `--dart-define=API_HOST` or `adb reverse` | Resolves `http://127.0.0.1:8000/api/v1/health` (adb reverse) or `http://<LAN_IP>:8000/api/v1/health`, HTTP 200 OK | `ApiConfig.baseUrl` dynamically resolves and logs source | `SIMULATED_ONLY` | `/api/v1/health` and `/health` aliases operational |
| **6. GPS Location Tracking** | `POST /api/v1/intelligence/risk-evaluate` | `LocationService`, `SafetySensorManager` | `ACCESS_FINE_LOCATION`, `ACCESS_COARSE_LOCATION` | Optional for foreground | Walk physically outdoors with phone | Coordinates update continuously, UI displays `GPS ACTIVE ±Xm`, speed & accuracy reflect reality | Implemented with `geolocator` stream; requires physical satellite lock | `NOT_TESTED` | Needs outdoor walking test on physical phone |
| **7. Accelerometer Processing** | Local pipeline | `SensorService`, `SafetySensorManager` | None | Foreground Service | Normal pickup, table placement, normal walking | Sensor values stream live ($X, Y, Z, \text{mag}$); normal handling evaluated as NORMAL, not false fall | `MultiStageFallDetector` enforces freefall + impact + stillness thresholds | `SIMULATED_ONLY` | `api_integration_test.dart` pickup ($12\text{ m/s}^2$) -> no fall |
| **8. Gyroscope Angular Tracking** | Local pipeline | `SensorService`, `SafetySensorManager` | None | Foreground Service | Rotate and tilt device normally | Angular velocity ($\text{rad/s}$) tracked; normal rotation does not trigger false alerts | 3-axis rotation stream integrated with high-pass filtering | `SIMULATED_ONLY` | Gyroscope rotation checks pass in test suite |
| **9. Real Voice Distress STT** | `POST /api/v1/ai/analyze-voice` | `VoiceService` | `RECORD_AUDIO` | Foreground Service (OS restricted) | Turn Guardian Mode ON, say "HELP ME EMERGENCY" into microphone | Real microphone records audio, SpeechToText parses "help me emergency", AI flags distress score | Real `SpeechToText` STT implemented; distinguished from `TEST_SIMULATOR` button | `NOT_TESTED` | Physical speech recognition requires physical device mic |
| **10. Multi-Signal Risk Engine** | `POST /api/v1/intelligence/risk-evaluate` | `GuardianRiskEngine`, `SosEscalationEngine` | None | None | Inject single sensor anomaly (e.g. table tap) vs multi-signal (Fall + Immobility) | Single sensor NEVER triggers SOS. Multi-signal elevates risk and triggers verification prompt | Verified by invariant tests; multi-factor fusion strictly enforced | `SIMULATED_ONLY` | `api_integration_test.dart`: single sensor signals rejected |
| **11. Risk Score UI Breakdown** | UI rendering | `RiskBreakdownCard`, `GuardianRiskEngine` | None | None | Trigger distinct risk factors (night, deviation, crime zone) | UI displays exact percentage and itemized bullet points explaining WHY score changed | `RiskBreakdownCard` renders linear gauge + itemized factors | `SIMULATED_ONLY` | Tested in `test/api_integration_test.dart` |
| **12. Route Destination Search** | `POST /api/v1/guardian/route` | `DestinationSearchSheet`, `GuardianRepository` | Internet | None | Search "Tambaram", "Airport", or arbitrary location | Search shows exact destination, user selects it, route polyline calculated to Tambaram (never replaced with Marina/Besant) | Dynamic geocoding via Nominatim & curated Chennai list | `SIMULATED_ONLY` | `DestinationSearchSheet` returns exact tapped coordinates |
| **13. Route Deviation Watchdog** | `POST /api/v1/intelligence/deviation-check` | `RouteDeviationDetector` | `ACCESS_FINE_LOCATION` | Foreground Service | Walk > 100m away from planned corridor for > 45s | UI triggers "Route Deviation Detected", presents check-in dialog, does not immediately SOS | Perpendicular Cartesian projection + 45s temporal filter | `SIMULATED_ONLY` | 140.8m off route triggers 45s confirmation window |
| **14. Map Layers & POIs** | `GET /api/v1/safety/zones`, `GET /api/v1/safety/police` | `GuardianMapController`, `GoogleMap` | Internet | None | Open Map screen and toggle Police / Hospitals / Safe Zones layers | Real Chennai police stations and safety zones render with clickable markers | Seeded with 34 Chennai Police Stations & 10 Safety Zones | `SIMULATED_ONLY` | `GuardianMapController` loads real DB layers |
| **15. Live Journey Lifecycle** | `POST /api/v1/journeys/start`, `POST /api/v1/journeys/{id}/safe-arrival` | `LiveJourneyScreen`, `SafeArrivalDetector`, `EtaSpeedWatchdog` | `ACCESS_FINE_LOCATION` | Foreground Service | Start journey -> travel on route -> reach within 80m of destination | Shows live speed, ETA, route status; reaching destination prompts safe arrival and completes | Multi-stage lifecycle from start to safe arrival verified | `SIMULATED_ONLY` | Tested in `test/api_integration_test.dart` |
| **16. Background Service** | OS Foreground Notification | `BackgroundSafetyService` | `POST_NOTIFICATIONS`, `FOREGROUND_SERVICE` | Android Foreground Service | Start Guardian Mode and minimize app / lock screen | Ongoing notification stays in tray with live GPS accuracy & battery %; monitoring continues | Implemented via `flutter_local_notifications` persistent foreground channel | `NOT_TESTED` | Needs verification on physical OEM battery saver |
| **17. Push Notifications** | `POST /api/v1/notifications/device-token` | `FcmNotificationService` | `POST_NOTIFICATIONS` | Google Play Services | Launch app on physical device with Google Play Services | Obtains real FCM token, registers with backend, displays foreground banners | `FcmNotificationService` implemented; requires physical device with Play Services | `NOT_TESTED` | Physical token generation requires physical device |
| **18. Contact Delivery Lifecycle** | `POST /api/v1/notifications/dispatch-emergency` | `NotificationDeliveryService` | SMS / Internet | None | Trigger emergency alert to trusted contacts | UI tracks per-contact status: `PENDING` -> `DISPATCHING` -> `DELIVERED` with latency | Delivery lifecycle model and state machine verified | `SIMULATED_ONLY` | Tested in `test/api_integration_test.dart` |
| **19. SOS Verification & Countdown** | `POST /api/v1/guardian/sos` | `SosEscalationEngine`, `SosDialog` | None | None | Trigger SOS manually or via multi-signal escalation | 20-second audible countdown with "I'M OK" cancellation button; 5-min cooldown prevents duplicates | 20s countdown and 5-min duplicate throttling active | `SIMULATED_ONLY` | Tested in `test/api_integration_test.dart` |
| **20. False Positive Feedback Loop** | `POST /api/v1/intelligence/false-positive` | `FalseAlarmManager`, `MultiStageFallDetector` | None | None | Cancel 2 false fall alerts within 24 hours | ML sensitivity increases fall impact threshold by +10% ($25.0 \to 27.5\text{ m/s}^2$), saves to SecureStorage | Dynamic adaptation verified; borderline motions suppressed | `SIMULATED_ONLY` | Tested in `test/api_integration_test.dart` |
| **21. Diagnostics Screen Truth** | Telemetry APIs | `DiagnosticsScreen` | `RECORD_AUDIO`, `ACCESS_FINE_LOCATION` | None | Open Diagnostics Screen | Live sensor values stream, STT status shown, API health pinged, simulation buttons clearly demarcated | Diagnostics screen strictly separates REAL streams from SIMULATED workbench | `SIMULATED_ONLY` | Inspected and verified in `diagnostics_screen.dart` |
| **22. Explainable Safety Score** | `GET /api/v1/dashboard` | `SafetyScoreCalculator` | None | None | Calculate safety score for daytime vs late night + low battery | Daylight: 100 (Excellent); Night + Crit Battery + High Risk + Weather: 40 (Low) | Deterministic multi-factor calculation verified | `SIMULATED_ONLY` | Tested in `test/api_integration_test.dart` |
